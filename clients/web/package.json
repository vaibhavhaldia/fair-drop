{
  "name": "fair-drop-web",
  "version": "1.0.0",
  "description": "The display — renders event/allocation state from the module (fixtures in Gate 1) per docs/CONTRACT.md §3",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "preview": "node --experimental-strip-types src/preview.ts"
  },
  "license": "ISC",
  "devDependencies": {
    "typescript": "~5.6.2",
    "vitest": "4.1.11"
  }
}
